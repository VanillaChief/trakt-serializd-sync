# trakt-serializd-sync configuration
# Copy to ~/keys/trakt-serializd-sync.env and fill in values

# Serializd credentials (required for auth)
SERIALIZD_EMAIL=
SERIALIZD_PASSWORD=

# Sync settings (optional - these are defaults)
# SYNC_INTERVAL_MINUTES=15
# CONFLICT_STRATEGY=trakt-wins  # trakt-wins, serializd-wins, newest-wins
# SYNC_DIRECTION=both           # both, trakt-to-serializd, serializd-to-trakt

# Rate limiting (optional)
# SERIALIZD_DELAY_MS=200        # Delay between Serializd API calls

# Logging (optional)
# LOG_LEVEL=INFO                # DEBUG, INFO, WARNING, ERROR
